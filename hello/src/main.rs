use regex::Regex;
use std::collections::HashMap;
use std::io::Write;

fn main() {
    // let regex = Regex::new("foo.*").unwrap();
    println!("Hello, world!");
    std::thread::sleep_ms(100);

    std::thread::sleep_ms(100);
}
